# Third-party notices

The generated Gradle wrapper scripts and `gradle/wrapper/gradle-wrapper.jar` are provided by the Gradle project under the Apache License 2.0. The license is included in `licenses/Gradle-LICENSE.txt`; generated script copyright headers are retained.

The Android application contains the project's own chess, vision and UI code and uses Android platform APIs. No pretrained network, model weights, reference photographs, third-party chess-piece artwork, or external recognition SDK is bundled.

Android SDK/build tools, AGP and Gradle are development dependencies downloaded by the build toolchain; their respective licenses apply. GitHub Actions used by CI are referenced at fixed commit SHAs, with their upstream repositories retained in the workflow.
